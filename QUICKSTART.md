# Быстрый старт

## 📁 Структура проекта

Проект находится в папке `telegram-calendar-bot` со следующей структурой:

```
telegram-calendar-bot/
├── main.py              # Основной файл бота
├── database.py          # Модуль для работы с PostgreSQL
├── requirements.txt     # Зависимости Python
├── README.md            # Полная документация
├── env.example          # Шаблон файла конфигурации
├── .gitignore          # Настройки Git
└── DEPLOY.md           # Инструкция по деплою на GitHub
```

## 🚀 Установка и запуск

1. **Перейдите в папку проекта:**
   ```bash
   cd telegram-calendar-bot
   ```

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Создайте файл `.env`** на основе `env.example`:
   ```bash
   copy env.example .env
   ```
   Затем отредактируйте `.env` и укажите:
   - `TELEGRAM_TOKEN` - токен вашего бота
   - Настройки PostgreSQL (DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD)

4. **Запустите бота:**
   ```bash
   python main.py
   ```

## 📤 Публикация на GitHub

1. **Создайте репозиторий на GitHub** (см. DEPLOY.md)

2. **Подключите remote и отправьте код:**
   ```bash
   cd telegram-calendar-bot
   git remote add origin https://github.com/YOUR_USERNAME/telegram-calendar-bot.git
   git branch -M main
   git push -u origin main
   ```

## 📝 Дополнительная информация

- Полная документация: `README.md`
- Инструкция по деплою: `DEPLOY.md`

