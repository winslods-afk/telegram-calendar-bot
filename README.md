# Telegram бот для мониторинга Apple Calendar (iCloud)

Telegram-бот, который автоматически отслеживает события из Apple Calendar (iCloud) и отправляет уведомления в Telegram. Поддерживает работу с несколькими пользователями, каждый из которых может подключить свой календарь.

## 🔧 Основные функции

- ✅ Подключение к Apple Calendar через CalDAV API
- ✅ Периодическая проверка календаря на новые события
- ✅ Автоматическая отправка уведомлений в Telegram
- ✅ Интерактивная настройка через Telegram (не требуется редактировать .env)
- ✅ Работа с несколькими пользователями одновременно
- ✅ Команда `/next` для получения ближайших 3 событий
- ✅ Защита от дублирования уже отправленных событий
- ✅ Хранение данных в PostgreSQL
- ✅ Логирование всех действий

## 📋 Требования

- Python 3.11 или выше
- PostgreSQL 12 или выше
- Apple ID с включенным двухфакторной аутентификацией
- App-Specific Password для iCloud
- Telegram Bot Token

## 🚀 Установка

1. Клонируйте репозиторий или скачайте файлы проекта

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Установите и настройте PostgreSQL:
   - Установите PostgreSQL на вашем сервере
   - Создайте базу данных:
   ```sql
   CREATE DATABASE calendar_bot;
   ```

4. Создайте файл `.env` на основе `env.example` и заполните его:

```env
# Telegram Bot Token (обязательно)
TELEGRAM_TOKEN=your_telegram_bot_token_here

# Настройки PostgreSQL (обязательно)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=calendar_bot
DB_USER=postgres
DB_PASSWORD=postgres

# Интервал проверки событий в минутах (опционально, по умолчанию 60)
CHECK_INTERVAL_MINUTES=60
```

## 🔑 Получение Telegram Bot Token

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в `.env` файл

**Важно:** Только токен бота нужен в `.env`. Данные пользователей (Apple ID, пароли) настраиваются через самого бота!

## 🎯 Запуск

Запустите бота одной командой:

```bash
python main.py
```

При первом запуске бот автоматически создаст необходимые таблицы в базе данных.

## 💬 Использование бота

### Первая настройка

1. Найдите вашего бота в Telegram и отправьте команду `/start`
2. Бот спросит: "Хотите ли Вы получать события из своего календаря в этом чате?"
3. Нажмите кнопку **"✅ Да"**
4. Бот покажет инструкцию по получению App-Specific Password
5. Нажмите кнопку **"📝 Предоставить данные"**
6. Отправьте ваш Apple ID (email)
7. Отправьте App-Specific Password (16 символов без пробелов)

### Получение App-Specific Password

1. Перейдите на [appleid.apple.com](https://appleid.apple.com)
2. Войдите в свой аккаунт
3. В разделе "Безопасность" найдите "Пароли приложений"
4. Создайте новый пароль приложения для "Другое" или "Почта"
5. Скопируйте сгенерированный пароль (16 символов без пробелов)

**Важно:** Используйте именно App-Specific Password, а не обычный пароль от Apple ID!

### Команды бота

- `/start` - начать настройку или обновить данные календаря
- `/next` - получить ближайшие 3 события из календаря
- `/cancel` - отменить текущую операцию

### Обновление данных

Если нужно обновить данные календаря:
1. Отправьте команду `/start`
2. Нажмите кнопку **"🔄 Обновить данные"**
3. Следуйте инструкциям

## 📝 Пример сообщения в Telegram

```
📅 Новое событие в календаре:

Название: Встреча с клиентом
Когда: 14 ноября, 10:00–11:00
Место: Офис на Арбате
Описание: Обсудить договор и сроки проекта
```

## ⚙️ Настройки

В файле `.env` можно настроить:

- `TELEGRAM_TOKEN` - токен вашего Telegram бота (обязательно)
- `DB_HOST` - хост PostgreSQL (по умолчанию `localhost`)
- `DB_PORT` - порт PostgreSQL (по умолчанию `5432`)
- `DB_NAME` - имя базы данных (по умолчанию `calendar_bot`)
- `DB_USER` - пользователь PostgreSQL (по умолчанию `postgres`)
- `DB_PASSWORD` - пароль PostgreSQL (по умолчанию `postgres`)
- `CHECK_INTERVAL_MINUTES` - интервал проверки событий в минутах (по умолчанию 60)

## 🗄️ Структура базы данных

Бот автоматически создает следующие таблицы:

- `users` - информация о пользователях и их настройках календаря
- `sent_events` - отслеживание уже отправленных событий (для предотвращения дублирования)

## 📊 Логирование

Все действия бота логируются в:
- Консоль (stdout)
- Файл `bot.log`

## 🔒 Безопасность

- **НЕ** коммитьте файл `.env` в репозиторий (он уже добавлен в `.gitignore`)
- Пароли пользователей хранятся в зашифрованной базе данных PostgreSQL
- Регулярно обновляйте пароли приложений
- Используйте сильные пароли для базы данных PostgreSQL

## 🐛 Решение проблем

### Ошибка подключения к базе данных

- Убедитесь, что PostgreSQL запущен
- Проверьте правильность настроек подключения в `.env`
- Убедитесь, что база данных создана
- Проверьте права доступа пользователя PostgreSQL

### Ошибка подключения к календарю

- Убедитесь, что используете App-Specific Password, а не обычный пароль
- Проверьте правильность Apple ID
- Убедитесь, что у вас включена двухфакторная аутентификация
- Попробуйте создать новый App-Specific Password

### Бот не отправляет сообщения

- Проверьте правильность `TELEGRAM_TOKEN` в `.env`
- Убедитесь, что вы настроили календарь через команду `/start`
- Проверьте логи в файле `bot.log`

### События не находятся

- Убедитесь, что в календаре есть события
- Проверьте, что события находятся в будущем (бот ищет события на ближайшие 7 дней)
- Убедитесь, что календарь правильно подключен

## 🔄 Обновление данных пользователя

Если нужно изменить настройки календаря:
1. Отправьте `/start` боту
2. Нажмите "🔄 Обновить данные"
3. Введите новые данные

## 📄 Лицензия

Этот проект создан для личного использования.
